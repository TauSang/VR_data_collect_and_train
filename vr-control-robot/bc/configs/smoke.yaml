seed: 42

data:
  raw_jsonl: ../data_collector/collector2/vr-demonstrations-episodes-20260313_142003.jsonl
  h5_path: ./artifacts/datasets/smoke_train.h5
  use_joint_velocities: true
  include_gripper: false
  joint_names:
    - leftShoulder
    - leftUpperArm
    - leftLowerArm
    - leftHand
    - rightShoulder
    - rightUpperArm
    - rightLowerArm
    - rightHand

train:
  batch_size: 128
  epochs: 5
  lr: 0.0003
  weight_decay: 0.00001
  val_ratio: 0.1
  log_interval: 10
  ckpt_dir: ./artifacts/checkpoints_smoke

model:
  hidden_dims: [256, 256]
  dropout: 0.1

export:
  out_path: ./artifacts/exported/policy_bc_smoke.pt
